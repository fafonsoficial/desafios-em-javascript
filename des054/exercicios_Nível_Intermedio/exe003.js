
/*
function multiplicarPorDois(numeros) {
    
    const resultado = numeros.map(function(numero) {
      return numero * 2;
    });
    return resultado;
  }

  const arrayOriginal = [1, 2, 3, 4, 5];
  const arrayMultiplicado = multiplicarPorDois(arrayOriginal);

  console.log('Array Original:', arrayOriginal);
  console.log('Array Multiplicado:', arrayMultiplicado);
  */

function funElement(n) {
    const res = n.map(function(n) {
        return n * 2;
    });
    
}
  